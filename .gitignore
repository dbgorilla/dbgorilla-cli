# Binary (anchored to repo root; keeps cmd/dbgorilla/ source tracked)
/dbgorilla
/dbgorilla-cli
/dbg

# OS
.DS_Store

# IDE
.idea/
.vscode/
*.swp

# Test
coverage.out

# Internal notes for agents/humans working here. CLAUDE.md is public and
# contributor-facing; anything that must not be published goes in the local
# file instead.
CLAUDE.local.md
.claude/settings.local.json

# Local tooling state: machine-specific, regenerated, never useful to a reader
# of this repo. Scoped deliberately -- .claude/rules/ stays tracked.
/.dbg/
/.claude/settings.json
